## Readme

### Execution

* clone the current repo
* copy the Classes\Config\DBConfig.php-dist to DBConfig.php
* update the variables with proper credentials to access the db
* execute the run.php with cli `php run.php` on 20th
* execute the rollback.php with cli `php rollback.php` on 28th 